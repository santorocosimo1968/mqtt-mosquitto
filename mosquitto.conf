# Listener sulla porta 1883 (senza TLS)
listener 1883
protocol mqtt

# Permetti connessioni da qualsiasi IP
allow_anonymous false

# File per autenticazione
password_file /mosquitto/config/password.txt

# Persistenza disabilitata (solo tempo reale)
persistence false

# Log
log_dest stdout
log_type all
